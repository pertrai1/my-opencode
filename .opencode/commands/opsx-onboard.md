---
description: "Guided onboarding for OpenSpec"
---

Use `sdlc-orchestrator` as the workflow owner for this command.

Intent: walk through an OpenSpec workflow cycle with narration while preserving the orchestrator's real lifecycle rules.

Input: `$ARGUMENTS`

Requirements:
- Preserve this command's user-facing intent: guided onboarding with narration.
- Use the orchestrator's real store-selection, planning, implementation, verification, and archive gates instead of a parallel onboarding workflow.
- Let `sdlc-orchestrator` narrate what it is doing and why at each stage.
