---
description: Run a full, coverage-led security audit of the current repository or a supplied scope.
agent: build
---

Load the `security-audit` skill now and follow its full-audit workflow. Audit the active repository unless the appended text names a different target or limits the scope. Preserve the skill's sandbox, write-isolation, validation, and reporting requirements.
