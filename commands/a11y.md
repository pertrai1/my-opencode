---
description: Build or review the current interface for WCAG 2.2 AA accessibility.
agent: build
---

Load the `frontend-a11y` skill now. Apply its accessibility workflow to the active repository unless the appended text provides a scope. For an audit, report evidence-based findings and required human validation; for implementation, make the requested frontend change conform to the skill's guidance.
